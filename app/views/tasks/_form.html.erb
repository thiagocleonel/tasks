<div class="panel panel-default">
    <div class="panel-heading">

    </div>
    <div class="panel-body">
        <div class="col-lg-6">

          <%#= form_with(model: task, :remote => true)  do |form| %>
          <%= form_with(model: task, local: true) do |form| %>
            <% if task.errors.any? %>
              <div id="error_explanation">
                <h2><%= pluralize(task.errors.count, "error") %> prohibited this movie from being saved:</h2>

                <ul>
                <% task.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
                </ul>
              </div>
            <% end %>

            <div class="field">
              <%= form.label :Título %>
              <%= form.text_field :title %>
            </div>

            <div class="field">
              <%= form.label :Descrição %>
              <%= form.text_field :description %>
            </div>

            <div class="field">
              <%= form.label :Entrega %>
              <%= form.date_field :delivery %>
            </div>

            <div class="actions">
              <%= form.submit %>
            </div>
          <% end %>

        </div>
    </div>
</div>
