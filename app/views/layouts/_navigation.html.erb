<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to t(:tasks), tasks_path(),class:"navbar-brand" %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item active">
          <%= link_to t(:home), tasks_path(),class:"navbar-brand" %>
        </li>
      <% end %>
    </ul>
    <form class="form-inline my-2 my-lg-0">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <% if user_signed_in? %>
            <%= link_to t("sign_out"), destroy_user_session_path(),class:"nav-link", method: :delete  %>
          <% else %>
            <%= link_to t("sign_up"), new_user_registration_path,class:"nav-link" %>
          <% end %>
        </li>
      </ul>
    </form>
  </div>
</nav>
